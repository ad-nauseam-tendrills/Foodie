{
  "name": "foodie",
  "version": "1.0.0",
  "private": true,
  "description": "Self-hosted, ad-free 'what's for dinner' app. Ingredient-based recipe matching. No accounts on someone else's server, no subscription, no data leaving your machine.",
  "type": "commonjs",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "seed": "node server/seed/seed.js",
    "seed:american": "node server/seed/seed-american.js",
    "seed:all": "npm run seed && npm run seed:american"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
